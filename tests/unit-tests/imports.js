// Imports for testing with node

var assert = require('assert');
var runtime = require('./../../lib/test-support');
var sys = require('sys');

var control = runtime.control;
var types = runtime.types;

