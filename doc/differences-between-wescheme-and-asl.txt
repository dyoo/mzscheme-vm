Here are deviations from ASL:


unmatched cond falls through

unmatched case falls through

branch expressions are not test for booleans at test position.

+, *, / don't take at least two arguments.

and, or, nand, nor don't require at least two arguments

set! allowed on function arguments

syntactic keywords can be used as variable names



missing primitives: 

    delay,
    check-member-of,
    check-range,
    check-error,
    exit,
    force,
    gensym,
    promise,
    pretty-print
    print
    read
    with-input-from-file
    with-input-from-string
    with-output-to-file
    with-output-to-string

    hash-copy
    hash-count
    hash-eq?
    hash-equal?
    hash-eqv?
    hash-has-key?
    hash-ref!
    hash-update!
    make-hasheqv
